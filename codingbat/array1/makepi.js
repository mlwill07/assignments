//Return an int array length 3 containing the first 3 digits of pi, {3, 1, 4}.

function makePi() {
    array = [];
    array.push(3, 1, 4);
    return array;
}

console.log(makePi()); // → [3, 1, 4]
